<div class="audition">
    {{ content.field_date_audition }}
    {{ title_prefix }}
    {% if not page %}
        <h3{{ title_attributes }}>
            <a href="{{ url }}" rel="bookmark">{{ label }}</a>
        </h3>
    {% endif %}
    {{ title_suffix }}
    {{ content.field_video }}
</div>
